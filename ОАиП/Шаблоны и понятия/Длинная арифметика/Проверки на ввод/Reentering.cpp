// Запрос на повторный ввод или выход
/* 
        char choice;
        std::cout << "\nХотите выполнить ещё одно вычисление? (Введите да или нет): ";
        std::cin >> choice;
        std::cin.ignore(10000, '\n'); // Очистка буфера ввода

        if (choice != 'y' && choice != 'Y' && choice !='yes' && choice !='YES' choice != 'да' && choice != 'ДА') {
            std::cout << "Выход из программы.\n";
            break;
        }

        std::cout << "\n";
        */