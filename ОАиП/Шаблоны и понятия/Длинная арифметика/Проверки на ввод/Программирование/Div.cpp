#include <bits/stdc++.h>
using namespace std;
 
int main() 
{
    ios::sync_with_stdio(false); 
    cin.tie(NULL);
int n,k;
cin>>n>>k;
std::vector<int> vec(n);
for (int i=0;i<n;i++)
{
    int q;
    cin>>q;
    vec[i]=q;
}
std::sort(vec.begin(), vec.end());
for (int i=0;i<n;i++)
{
   
}
 return 0;
    
}